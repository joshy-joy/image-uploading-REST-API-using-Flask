1. Both front end and REST API are developed in flask

Steps to run
---------------

1. Activate the virtual environment 'venv'
    
    >> source venv/bin/activate

2. run the file 'run.py' to start REST API

    >>cd Rest\ Server/
    >>python run.py

3. run the client.py in Client Server to start front end interface

    >>cd ..
    >>cd Client\ Server/
    >>python client.py

4. open 'localhost:5001/' on web browser


Thanks you
